import { ClockFormat12Pipe } from './clock-format-12.pipe';

describe('ClockFormat12Pipe', () => {
  it('create an instance', () => {
    const pipe = new ClockFormat12Pipe();
    expect(pipe).toBeTruthy();
  });
});
