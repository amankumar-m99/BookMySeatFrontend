import { ClockFormat24Pipe } from './clock-format-24.pipe';

describe('ClockFormat24Pipe', () => {
  it('create an instance', () => {
    const pipe = new ClockFormat24Pipe();
    expect(pipe).toBeTruthy();
  });
});
