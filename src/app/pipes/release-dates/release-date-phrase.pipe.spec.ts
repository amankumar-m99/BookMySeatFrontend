import { ReleaseDatePhrasePipe } from './release-date-phrase.pipe';

describe('ReleaseDatePhrasePipe', () => {
  it('create an instance', () => {
    const pipe = new ReleaseDatePhrasePipe();
    expect(pipe).toBeTruthy();
  });
});
