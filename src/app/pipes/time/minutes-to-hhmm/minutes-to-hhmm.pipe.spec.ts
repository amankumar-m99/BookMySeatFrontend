import { MinutesToHhmmPipe } from './minutes-to-hhmm.pipe';

describe('MinutesToHhmmPipe', () => {
  it('create an instance', () => {
    const pipe = new MinutesToHhmmPipe();
    expect(pipe).toBeTruthy();
  });
});
